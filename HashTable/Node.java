/*
 * Dalton Carter
 * This class implements the nodes that will occupy the BSTs within the hash table.
 */
public class Node {

    public int iData;
    public Node leftChild;
    public Node rightChild;

    // Constructor.
    public Node(int key)
    { iData = key; }
}
